NAME            = lifemapper-cctools
VERSION         = 7.0.15
ARCHIVENAME     = cctools
PKGNAME         = $(ARCHIVENAME)-$(VERSION)-source
RELEASE         = 0
TARBALL_POSTFIX	= tar.gz

RPM.EXTRAS = "AutoReq: no"

RPM.FILES = $(PKGROOT)/$(ARCHIVENAME)/  
