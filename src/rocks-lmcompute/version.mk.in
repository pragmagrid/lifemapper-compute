NAME    = rocks-lmcompute
VERSION = $(CODEVERSION)
RELEASE = 1

TARBALL_POSTFIX   = tar.gz

RPM.FILES = \
    $(PKGROOT)/rocks/*  \n\
    /etc/ld.so.conf.d/lifemapper.conf  \n\
    /etc/profile.d/lmcompute.*
